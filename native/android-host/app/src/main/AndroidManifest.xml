<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Media plane (Chime) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Telecom self-managed VoIP ("recognised like WhatsApp") -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Call notifications / full-screen incoming UI -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Background call (Android 14 typed FGS: phoneCall + microphone) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application
        android:name=".HostApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChimeFlutterHost">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ChimeFlutterHost">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demo feature screen with its own call entry point (SupportCallLauncher). -->
        <activity
            android:name=".PaymentsActivity"
            android:exported="false"
            android:theme="@style/Theme.ChimeFlutterHost" />

        <!-- The Flutter call UI (add-to-app), launched with the cached engine. -->
        <activity
            android:name="io.flutter.embedding.android.FlutterActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.ChimeFlutterHost"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
