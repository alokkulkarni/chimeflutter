# Flutter module ephemeral projects — generated by `flutter pub get` / `flutter build`.
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
# The hidden add-to-app host projects are generated locally (do not commit):
.ios/
.android/
build/
