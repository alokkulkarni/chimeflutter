<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Media plane -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Telecom self-managed VoIP ("recognised like WhatsApp") -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Call notifications / full-screen incoming UI -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Keep the call alive in the background. Android 14 requires typed FGS permissions:
         phoneCall (Telecom) + microphone (RECORD_AUDIO). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application>
        <service
            android:name=".CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="phoneCall|microphone" />

        <!-- Answer/Decline actions of the incoming-call (simulated outbound) notification. -->
        <receiver
            android:name=".IncomingCallActionReceiver"
            android:exported="false" />
    </application>
</manifest>
